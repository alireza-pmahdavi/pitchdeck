
  # Caasify PitchDeck

  This is a code bundle for Caasify PitchDeck. The original project is available at https://www.figma.com/design/LEFygEwVS0BeUh7SCfita4/Caasify-PitchDeck.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  